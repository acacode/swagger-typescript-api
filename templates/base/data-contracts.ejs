<%
  // this template is generate 'data-contracts.ts' file
  const { modelTypes, utils } = it;
  const { formatDescription, require, _ } = utils;
%>
<% modelTypes.forEach((contract) => { %>

<%~ includeFile('@base/data-contract-jsdoc.ejs', { ...it, contract }) %>
<%~ includeFile(`@base/${contract.typeIdentifier}-data-contract.ejs`, { ...it, contract }) %>

<% }) %>
