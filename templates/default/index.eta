<% it.modelTypes.forEach((contractType) => { %>

    <%~ includeFile('./data-contract.eta', contractType) %>

<% }) %>

<% if (it.config.generateRouteTypes) { %>
<%~ includeFile('./route-types.eta', it) %>
<% } %>

<% if (it.config.generateClient) {%>
<%~ includeFile('./http-client.eta', it) %>

<%~ includeFile('./api.eta', it) %>
<% } %>