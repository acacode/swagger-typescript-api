openapi: "3.0.3"
info:
  version: "3.0.0"
  title: "App NG"

paths:
  /call1:
    get:
      operationId: "call1"
      parameters:
        - in: path
          name: pathParam1
          required: true
          schema:
            type: string
        - name: pathParam2
          in: path
          required: true
          schema:
            type: string
      responses:
        204:
          description: "Accepted"
  /call2:
    get:
      operationId: "call2"
      description: "This is a multiline
        description"
      parameters:
        - in: path
          name: pathParam
          required: true
          schema:
            type: string
      responses:
        204:
          description: "Accepted"