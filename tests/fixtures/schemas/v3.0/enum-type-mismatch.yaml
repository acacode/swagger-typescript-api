openapi: "3.0.0"
info:
  title: Enum Type Mismatch
  version: "1.0.0"
paths:
  /status:
    get:
      operationId: getStatus
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Models_ConsultStatus'
components:
  schemas:
    Models_ConsultStatus:
      type: integer
      format: int32
      enum:
        - Scheduled
        - CancelledByClient
